# Direct production dependencies.
# Versions are intentionally pinned to the CPython 3.12 production baseline.
# Claude review of a finished alignment + tree (tree_analysis_service).
anthropic==0.122.0
biopython==1.87
# Voucher Sync: per-user iNat token encryption at rest.
cryptography==50.0.0
Flask==3.1.3
Flask-Limiter==4.1.1
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.2
gunicorn==23.0.0
Jinja2==3.1.6
MarkupSafe==3.0.3
# Voucher Sync: QR decoding of observation photos (worker only; lazy-imported).
# rapidocr-onnxruntime requires the full opencv-python (not -headless), which
# needs libgl1 + libglib2.0-0 on the host. Pinned here so it does not float.
opencv-python==4.12.0.88
psutil==7.2.0
psycopg2-binary==2.9.11
# ITS1/5.8S/ITS2 region extraction (optional pipeline step); pulls in pyhmmer.
pyitsx==0.1.0
python-dotenv==1.2.2
# Voucher Sync: OCR fallback when no QR code decodes (worker only; lazy-imported).
rapidocr-onnxruntime==1.4.4
redis==7.1.0
requests==2.33.0
rq==2.6.1
